export enum StudentManager {
    PageSize = 5
}

export enum BAManager {
    PageSize = 5
}

export enum MorderationPostManager {
    PageSize = 5
}

export enum MorderationCourseManager {
    PageSize = 5
}
